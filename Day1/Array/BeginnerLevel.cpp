#include <iostream>
using namespace std;

int main(){
	
	/*
		Difficulty: Easy
		Topic: Arrays
		Title: Cookies Party
		
		Tips: 
		- this is how we declare new array:
			1) type name[size]; ===> int arr1[10];
			2) type name[size] = {values}; ===> int arr1[10] = {1,2,3,4,5,6,7,8,9,0};
			
		- this is how we declare new 2D array:
			1) type name[sizeRow][sizeCol]; ===> int arr1[10][10];
			2) type name[sizeRow][sizeCol] = {values}; 
					===> int arr1[2][3] = { {1,2,3}, {4,5,6} };
								1, 2, 3
								4, 5, 6
	*/
	
	// 1. Initialise an array, type=int, size=5, name=cookiesEachBag, initial value=1,2,3,4,5
	
	// 2. Print how many cookies in every bag
	
	// 3. Add 1 cookie to every bag in cookiesEachBag

	// 4. Each bag has owner. Declare an array, type=string, size=5, name=owner
	
	// 5. Let user input name one by one
	
	// 6. Print out how many cookies, with each person's name
	//  eg: Ali: 2, Muthu: 5,...
	
	// 7. Another groups of students come to party. Declare 2D array to combine owner array and new group.
	//		type=string, size=2x5, name=groupsOfOwners, new groups = Person1, Person2, Person3, Person4, Person5
	
	// 8. Print all owners' names
	
	// 9. Rename new group names by letting user to input. Only the new group, not the original group. 
	
	return 0;
}