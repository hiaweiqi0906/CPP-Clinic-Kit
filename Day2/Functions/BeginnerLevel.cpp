#include <iostream>
using namespace std;

int main(){
	
	/*
		Difficulty: Easy
		Topic: Functions 
		Title: Cookies Party - Extended Upgraded Version
		
		Tips: 
		- this is how we define function prototype:
            returnType functionName(parameterDataType); ===> int getSum(int, int);
	*/
	
	/*---------------------------------------------------------*\
	|															|
	|		Please paste your code from "Cookies Party" 		|
	|		[Pointers Exercise] here before you proceed!		|
	|															|
	\*---------------------------------------------------------*/
	
	// 19. Declare function prototype, function definition and call that function to print every owner name.
	//		return=none, name=printOwner, parameter=owner(1D array)
	
	// 20. Declare function prototype, function definition and call that function to get total number of cookies in bags.
	//		return=int, name=getTotal, parameter=cookiesEachBag(1D array)
	
	// 21. Declare similar function prototype, function definition with Q20 and call that function to get total number of cookies in bags.
	//		return=int, name=getTotal, parameter=cookiesPointer(Pointer)
	
	return 0;
}